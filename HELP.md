# 灵虾使用手册

> 灵虾是你的专属 AI 助手。能聊天、查资料、写分析、做 PPT、跑代码，也能和同事助手协作，还能连微信随时找它。

---

## 快速上手

### 第一次用，三步找到手感

1. **先聊一句简单的话**：比如"你好，你能做什么？"
2. **再试一个真实任务**：比如"帮我看下今天 A 股收盘情况"
3. **最后再试复杂任务**：比如"做一份 AI 金融应用研究提纲"

### 领养你的灵虾

1. 登录灵感平台
2. 进入 **灵感养虾**
3. 领养后，系统会分配一个专属子域名
4. 打开专属页面即可开始对话

每只灵虾都是独立的：对话、记忆、技能、文件互不混用。

---

## 侧边导航

打开灵虾后，左侧导航栏有以下入口：

| 图标 | 名称 | 说明 |
|------|------|------|
| 💬 | **聊天** | 主聊天页面，日常对话入口 |
| ✨ | **技能** | 查看和管理已安装的技能 |
| 📱 | **微信** | 绑定微信，随时和灵虾对话 |
| 🤖 | **代理** | 查看 Agent 概览、文件、配置 |
| ⏰ | **定时任务** | 设置自动执行的周期性任务 |
| 🌙 | **梦境记忆** | 灵虾的长期记忆档案 |
| 👥 | **智能体协作** | 跨助手的协作请求管理 |
| ⚙️ | **设置** | 助手名字、个性、上下文轮次等 |
| 📖 | **文档** | 帮助文档（就是你正在看的这份） |

---

## 聊天页面

### 基本用法

聊天页面是和灵虾互动的主要入口。支持：

- **文字对话**：直接打字提问
- **语音输入**：点麦克风按钮说话，灵虾自动转文字后回答
- **模型切换**：顶部可切换不同模型（GLM-5、MiniMax M2.7、DeepSeek 等）
- **重置会话**：清空当前对话上下文，重新开始（不会删除记忆和文件）

### 流式输出

灵虾会一边生成一边往外吐字，不需要等整段内容全部完成。生成过程中可以点 **停止** 按钮中断。

### 工具调用

灵虾在回答过程中可能会调用工具（搜索、执行代码等）。你会看到：

- 工具调用卡片：显示工具名称、执行状态、耗时
- 产出文件卡片：工具执行完后如果生成了文件，会直接显示下载按钮

### 连接方式

灵虾支持两种连接方式，自动选择：

- **WebSocket（WSS）**：优先使用，实时双向通信
- **HTTP SSE**：WSS 不可用时自动降级（如企业内网环境），功能完全一致

在浏览器控制台可以看到 `[DIAG]` 开头的日志，显示当前走的是哪条路径。

---

## 微信集成

### 绑定微信

1. 进入侧边栏 **微信** 页面
2. 点击绑定，扫描二维码
3. 确认后即绑定成功

### 使用方式

绑定后，你可以直接在微信对话框里和灵虾聊天：

- **发文字**：直接发消息，灵虾会回复
- **发语音**：微信自动转文字后灵虾处理，效果和打字一样
- **解绑**：在微信页面点击解绑即可

灵虾收到微信消息后会调用主聊天的 AI 能力回复，和网页端用的是同一个大脑。

---

## 技能

### 技能是什么

技能是给灵虾安装的专门能力模块。比如查行情、读研报、生成 PPT、搜资讯等。

### 在哪里看

侧边栏 **技能** 页面。每个技能会显示：

- 来源：平台技能 / 私有技能 / 扩展技能
- 状态：已绑定 / 未绑定 / 需配置 / 缺依赖

### 技能市场

技能页面右上角可以进入 **技能市场**，浏览和安装更多技能。

### 为什么开了技能还不一定能用

两个状态要分清：

- **已安装**：技能文件存在
- **已绑定**：技能绑定到了你的 Agent，对话中可以调用

有些技能安装了但没绑定、或缺运行环境/配置，就会"看得见但用不了"。

---

## 协作广场

### 怎么打开

聊天页右上角的 **协作** 按钮，或侧边栏 **智能体协作**。

### 业务 Agent

协作面板里可以直接和专用业务 Agent 对话，比如：

- **PPT 生成助手**：描述主题、受众、页数，自动生成演示文档
- **代码助手**：在隔离沙箱里写代码、调试、运行
- **金融投顾**：DCF 模型、行业分析、研究结论整理
- **信用风险 / 债券分析**（远程 Agent）：对接专业后端系统

业务 Agent 的会话独立管理，关闭面板后短时间内重新打开可继续。

### 跨助手协作

- **收到的请求**：别人调用你的助手时，可以批准/拒绝/查看执行过程
- **我发出的请求**：你调用别人的助手时，查看状态和结果

---

## 定时任务

侧边栏 **定时任务** 页面。可以设置灵虾定期自动执行任务：

- **每隔 N 分钟/小时**执行
- **每天固定时间**执行
- **Cron 表达式**精确控制

支持的任务类型：

- 发一条消息让灵虾处理（agentTurn）
- 系统事件触发（systemEvent）

每次执行的结果（摘要、token 用量、耗时）都可以在历史记录里查看。

---

## 文件与记忆

### 文件管理

在侧边栏 **代理** > **Files** 页面：

- 下载灵虾生成的文件（PPT、代码输出等）
- 查看和编辑记忆文件
- 管理工作区文件

### 灵虾会记住什么

直接告诉它：

- "记住我更关注港股和 AI 方向"
- "以后按这个格式输出"
- "把这个写进记忆里"

灵虾会写入记忆文件，后续对话自动参考。

### 梦境记忆

侧边栏 **梦境记忆** 页面可以查看灵虾的长期记忆档案。这些记忆会在对话中被自动引用。

---

## 语音能力

### 语音输入

聊天页面支持语音输入：点麦克风按钮 > 说话 > 自动转文字 > 灵虾回答。

### 语音合成（TTS）

灵虾的回复可以转成语音播放。

### 微信语音

在微信里直接发语音消息，灵虾会自动识别并回复文字。

---

## 设置

在侧边栏 **设置** 页面可以调整：

- 助手名字和个性风格
- 是否启用长期记忆
- 上下文轮次（保留多少轮历史对话）
- 模型偏好

如果觉得回答太跳、太短、太啰嗦，通常可以从这里开始调。

---

## 安全边界

### 灵虾能做什么

在授权范围内：聊天、调用技能、生成文件、执行代码（沙箱隔离）、协作。

### 灵虾不能做什么

- 读取别人的文件
- 获取系统密钥
- 越过隔离边界
- 修改系统级配置

### 代码执行安全

灵虾的代码执行在隔离沙箱中运行：

- 无网络访问
- 只读根文件系统
- CPU、内存、进程数、超时均有限制
- 5 分钟执行超时自动中断

---

## 常见问题

### 技能开了但感觉没效果？

1. 检查技能是否"已绑定"（不只是已安装）
2. 重置会话后再试

### PPT 生成失败怎么办？

把需求说得更具体：主题、受众、页数、风格偏好。越具体成功率越高。

### 想让它记住某件事？

直接说"记住xxx"或"以后按xxx方式回答我"。

### 微信发语音没反应？

确认已绑定微信。语音消息由微信自动转文字后处理，和打字效果一样。

### 在企业内网用很慢或消息结束不了？

企业网络可能屏蔽 WebSocket，灵虾会自动降级到 HTTP。如果遇到异常，尝试刷新页面重试。

### 会话到期了数据还在吗？

- 对话按会话生命周期管理
- 记忆文件长期保留
- 已生成的文件通常也还在

---

## 最后

灵虾不只是一个聊天框。日常用它聊天查资料，复杂任务交给专用 Agent，长期偏好交给记忆，协作任务放进协作广场，微信随时找它——这样最顺手。
