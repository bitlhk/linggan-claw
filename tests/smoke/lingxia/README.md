# Legacy Lingxia Smoke Entry

This directory is kept for backward compatibility. New smoke tests live in `tests/smoke/employee-agent/`.

Use:

```bash
node tests/smoke/employee-agent/playwright-runner.mjs
```

Older imports from this directory continue to work through wrapper files.
